package wangjm.java.base;

public class StringTrain {
    public static void main(String[] args) {
        String name = "Alan";

        /*
        * 1.length:返回字符串字符个数
        * 2.isEmpty:判断是否是空字符串
        * 3.charAt(int index):返回字符串某个位置字符
        * 4.toCharArray():将字符串变为字符数组
        * 5.indexOf():查找某个字符或字符串首次出现的位置
        * 6.lastIndexOf():查找某个字符或字符最后一次出现的位置
        * 7.toUpperCase()，toLowerCase:转化为大写或小写
        * 8.String[] split(String regex) :根据分隔符拆分字符串，且将拆分后的字符串放入字符串数组中
        * 9.equals(Object object):比较两个字符串值是否相等
        * 10.trim()：去掉字符串左右空格
        * 11.replace(target,newChar):新字符串替换就字符串
        * 12.subString(int beginIndex):从字符串指定位置处截取字符串
        * 13.equalsIgnoreCase(string):忽略两个字符缓大小写比较
        * 14.contains():判断某个字符串是否包含指定字符或字符串
        * 15.startsWith(String str),endWith(String string):测试某字符串是否以指定前缀开始，或结束
        * */
    }
}
